# dependencies
node_modules/

# env files (never commit secrets)
.env
.env.local

# build output
dist/
build/

# database (created locally by `npm run seed` / running the API)
backend/portfolio.db

# editor / OS
.vscode/*
!.vscode/extensions.json
.DS_Store

.vercel
