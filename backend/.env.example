PORT=4000
# Must match the origin your frontend runs on (Vite's default dev port).
CORS_ORIGIN=http://localhost:5173
# Simple shared secret to view submitted contact messages — change this.
ADMIN_KEY=changeme123
